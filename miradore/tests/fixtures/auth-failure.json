{
  "name": "auth-failure",
  "integration": "miradore",
  "description": "A rejected key must end the run at the path probe, in one request. No attribute list and no other request path fixes a bad credential, so neither ladder should be walked. request_count of 1 is what carries this.  The task ENDS IN ERROR. That is what separates this from an empty site: both report zero assets, and only the exit status tells them apart.",
  "kwargs": {
    "url": "$BASE",
    "api_key": "1_wrongkey",
    "site_name": "acme",
    "include_v2_details": "false"
  },
  "routes": [
    {
      "name": "unauthorized",
      "match": {
        "method": "GET",
        "path": "/acme/API/Device"
      },
      "responses": [
        {
          "status": 401,
          "headers": {
            "Content-Type": "application/xml"
          },
          "text": "<Content><Error><Description>Authentication failed</Description><Code>401</Code></Error></Content>"
        }
      ],
      "when_exhausted": "repeat_last"
    }
  ],
  "expect": {
    "asset_count": 0,
    "request_count": 1,
    "script_error": true,
    "error_contains": [
      "could not read the device list",
      "status 401: the authentication key was rejected"
    ]
  },
  "invariants": {
    "skip": []
  }
}
