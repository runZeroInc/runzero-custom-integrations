{
  "name": "auth-failure",
  "integration": "slurpit",
  "description": "A revoked or mistyped API key. The /api/platform/ping preflight answers 401, and the run must end there with a diagnostic naming the key -- one request, no device walk, no assets, no abort. This is the branch the paged scenario never touches.",
  "kwargs": {
    "url": "$BASE",
    "api_key": "revoked-fixture-key"
  },
  "routes": [
    {
      "match": {"method": "GET", "path": "/api/platform/ping"},
      "responses": [
        {"status": 401, "json": {"error": "unauthorized"}}
      ],
      "when_exhausted": "repeat_last"
    }
  ],
  "default_response": {"status": 404, "text": "no fixture route"},
  "expect": {
    "asset_count": 0,
    "request_count": 1,
    "requests_include": [
      {"method": "GET", "path": "/api/platform/ping", "header_contains": "Bearer revoked-fixture-key"}
    ],
    "requests_absent": [
      {"path_contains": "/api/devices"},
      {"path_contains": "/api/sites"}
    ],
    "log_contains": [
      "slurpit: the API key was rejected by /api/platform/ping"
    ]
  }
}
