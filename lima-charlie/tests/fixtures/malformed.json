{
  "name": "malformed",
  "integration": "lima-charlie",
  "description": "The sensors endpoint answers with a bare JSON array where an object carrying \"sensors\" is documented. Reading .get off a list aborts the script outright, so the type guard has to stop the run cleanly instead. Proves zero assets, one log line, and no crash.",
  "kwargs": {
    "url": "$BASE",
    "jwt_url": "$BASE",
    "organization_id": "8bd1f0e2-0000-4000-8000-000000000001",
    "api_token": "fixture-lc-secret"
  },
  "routes": [
    {
      "name": "jwt-exchange",
      "match": {"method": "POST", "path": "/"},
      "responses": [{"status": 200, "json": {"jwt": "fixture-lc-jwt"}}],
      "when_exhausted": "error"
    },
    {
      "name": "sensors-wrong-shape",
      "match": {"method": "GET", "path": "/sensors/8bd1f0e2-0000-4000-8000-000000000001"},
      "responses": [
        {"status": 200, "json": [{"sid": "aaaa0001-0000-4000-8000-000000000001"}]}
      ],
      "when_exhausted": "error"
    }
  ],
  "expect": {
    "asset_count": 0,
    "request_count": 2,
    "log_contains": ["Failed to fetch sensors: unexpected response shape, wanted an object"]
  },
  "invariants": {"skip": []}
}
