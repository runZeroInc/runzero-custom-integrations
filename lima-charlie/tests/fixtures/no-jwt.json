{
  "name": "no-jwt",
  "integration": "lima-charlie",
  "description": "The token exchange succeeds at the HTTP level but returns no \"jwt\". bearer() rejects None and would abort the script, so the run must stop at the token check and never reach the sensors endpoint. Proves the guard fires and that no unauthenticated data call is attempted.",
  "kwargs": {
    "url": "$BASE",
    "jwt_url": "$BASE",
    "organization_id": "8bd1f0e2-0000-4000-8000-000000000001",
    "api_token": "fixture-lc-secret"
  },
  "routes": [
    {
      "name": "jwt-exchange-empty",
      "match": {"method": "POST", "path": "/"},
      "responses": [{"status": 200, "json": {"error": "oid not permitted for this secret"}}],
      "when_exhausted": "error"
    }
  ],
  "default_response": {"status": 404, "text": "no fixture route"},
  "expect": {
    "asset_count": 0,
    "request_count": 1,
    "requests_absent": [{"path_contains": "/sensors/"}],
    "log_contains": ["No JWT returned for organization 8bd1f0e2-0000-4000-8000-000000000001"]
  },
  "invariants": {"skip": []}
}
