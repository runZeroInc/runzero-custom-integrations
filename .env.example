# Credentials for the live endpoint tests (python3 tests/run_live.py).
#
#   cp .env.example .env      # .env is git-ignored; .env.example is not
#
# Then uncomment and fill in the block for each integration you can actually
# reach. An integration runs only when its variables are set, so a developer
# with two credentials configured sees two runs and one skip count, not a
# failure for every other integration in the repo. NEVER commit .env, and never
# put a real credential in this file.
#
# Regenerate this file after a CONFIG change:
#
#   python3 tests/run_live.py --env-template > .env.example
#
# ===========================================================================
# Globals
# ===========================================================================

# The runzero CLI that runs each script. Defaults to `runzero` on PATH.
#RUNZERO_CLI=/opt/runzero/bin/runzero

# ===========================================================================
# Naming
# ===========================================================================
#
# Every variable is <PREFIX>_<PARAMETER>, where PARAMETER is the CONFIG
# parameter key upper-cased -- so the integration's own CONFIG block is the
# only declaration of what it accepts, and there is no second list to drift.
# PREFIX defaults to the directory name upper-cased
# (ubiquiti-unifi-network -> UBIQUITI_UNIFI_NETWORK) and an integration may
# claim a shorter one in <slug>/tests/live.json:
#
#   {"env_prefix": "UNIFI", "aliases": {"TLS_INSECURE": "tls_disable_validation"}}
#
# A <PREFIX>_ variable that matches no parameter is an error, not a shrug:
# a typo'd credential that is silently ignored still reports a green run.
#
# Three suffixes belong to the harness rather than to an integration:
#
#   <PREFIX>_EXPECT        what a valid result looks like (required)
#   <PREFIX>_EXPECT_FILE   the same terms, one per line, from a file outside git
#   <PREFIX>_LIVE_TIMEOUT  seconds before the run is failed (default 600)
#
# ===========================================================================
# Expectations
# ===========================================================================
#
# EXPECT is a space-separated list of terms, shell-quoted. Every term must
# parse: an unrecognised one fails the run rather than being ignored, because
# an expectation with a typo that silently passes is worse than none at all.
#
#   asset_count>=5              how many assets came back (also > = < <= ==)
#   services>=1                 services across every asset (also software,
#                               vulnerabilities)
#   any:hostnames=JOHNS-IPHONE  some asset has this value in this field
#   any:os~Windows              some asset's field contains this substring
#   asset[unifi:site:aa-bb]     this exact foreign id was emitted
#   asset[unifi:site:aa-bb]:deviceType=Switch      ... with this field value
#
# Field names are the ones on the EMITTED asset -- id, hostnames, ipAddresses,
# macAddresses, deviceType, os, and each <prefix>_ custom attribute -- not the
# vendor's field names. `=` matches one whole value case-insensitively, `~`
# matches a substring. Quote values containing spaces: any:os="Windows Server".
#
# Expectations are printed in failure messages, so put no secrets in them.
#
# ===========================================================================
# Integrations
# ===========================================================================

# --- Absolute Secure Endpoint (absolute) ------------------------------------
# Absolute API host (required)
#ABSOLUTE_API_HOST=https://api.absolute.com
# API token ID (required)
#ABSOLUTE_TOKEN_ID=replace-me
# API secret key (required)
#ABSOLUTE_SECRET_KEY=replace-me
# Import installed software
#ABSOLUTE_IMPORT_SOFTWARE=false
# Only import active agents
#ABSOLUTE_ACTIVE_ONLY=false
# Page size
#ABSOLUTE_PAGE_SIZE=500
# User-Agent
#ABSOLUTE_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#ABSOLUTE_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#ABSOLUTE_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#ABSOLUTE_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#ABSOLUTE_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#ABSOLUTE_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#ABSOLUTE_EXPECT='asset_count>=1'

# --- AdGuard Home (adguard-home) --------------------------------------------
# AdGuard Home URL (required)
#ADGUARD_HOME_URL=http://adguard.example.com:3000
# Username (required)
#ADGUARD_HOME_USERNAME=replace-me
# Password (required)
#ADGUARD_HOME_PASSWORD=replace-me
# Collect DHCP leases
#ADGUARD_HOME_COLLECT_DHCP=true
# Collect observed DNS clients
#ADGUARD_HOME_COLLECT_CLIENTS=true
# Import persistent clients with no observation
#ADGUARD_HOME_INCLUDE_PERSISTENT_ONLY=false
# Maximum clients
#ADGUARD_HOME_MAX_CLIENTS=20000
# User-Agent
#ADGUARD_HOME_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#ADGUARD_HOME_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#ADGUARD_HOME_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#ADGUARD_HOME_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#ADGUARD_HOME_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#ADGUARD_HOME_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#ADGUARD_HOME_EXPECT='asset_count>=1'

# --- akamai-guardicore-centra (akamai-guardicore-centra) --------------------
# This directory ships several scripts. Name the one to test with "script" in akamai-guardicore-centra/tests/live.json.
# What a valid result looks like (required to run; see tests/README.md)
#AKAMAI_GUARDICORE_CENTRA_EXPECT='asset_count>=1'

# --- HPE Aruba ClearPass (aruba-clearpass) ----------------------------------
# ClearPass URL (required)
#ARUBA_CLEARPASS_URL=https://cppm.example.com
# API client ID (required)
#ARUBA_CLEARPASS_CLIENT_ID=replace-me
# API client secret (required)
#ARUBA_CLEARPASS_CLIENT_SECRET=replace-me
# Endpoint status
#ARUBA_CLEARPASS_STATUS_FILTER=any
# Join active RADIUS sessions
#ARUBA_CLEARPASS_INCLUDE_SESSIONS=false
# Maximum profile age (days)
#ARUBA_CLEARPASS_MAX_PROFILE_AGE_DAYS=0
# Page size
#ARUBA_CLEARPASS_PAGE_SIZE=500
# User-Agent
#ARUBA_CLEARPASS_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#ARUBA_CLEARPASS_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#ARUBA_CLEARPASS_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#ARUBA_CLEARPASS_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#ARUBA_CLEARPASS_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#ARUBA_CLEARPASS_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#ARUBA_CLEARPASS_EXPECT='asset_count>=1'

# --- Asimily (asimily) ------------------------------------------------------
# Asimily portal URL (required)
#ASIMILY_URL=https://customer-portal.asimily.com
# API username (required)
#ASIMILY_USERNAME=replace-me
# API key (required)
#ASIMILY_API_KEY=replace-me
# Source header
#ASIMILY_SOURCE=runzero
# Import open CVEs
#ASIMILY_INCLUDE_VULNERABILITIES=true
# Import installed software
#ASIMILY_INCLUDE_SOFTWARE=false
# Software device limit
#ASIMILY_SOFTWARE_DEVICE_LIMIT=100
# Asset page size
#ASIMILY_PAGE_SIZE=200
# Request timeout (seconds)
#ASIMILY_REQUEST_TIMEOUT=180
# User-Agent
#ASIMILY_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#ASIMILY_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#ASIMILY_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#ASIMILY_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#ASIMILY_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#ASIMILY_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#ASIMILY_EXPECT='asset_count>=1'

# --- Audit Log to Webhook (audit-events-to-webhook) -------------------------
# runZero source URL
#AUDIT_EVENTS_TO_WEBHOOK_SRC_URL=https://vendor.example.com
# Webhook URL (required)
#AUDIT_EVENTS_TO_WEBHOOK_DST_URL=https://vendor.example.com
# Webhook API key
#AUDIT_EVENTS_TO_WEBHOOK_EXTERNAL_API_KEY=replace-me
# runZero account token (required)
#AUDIT_EVENTS_TO_WEBHOOK_RZ_ACCOUNT_TOKEN=replace-me
# Legacy JSON credential
#AUDIT_EVENTS_TO_WEBHOOK_LEGACY_CREDENTIALS=replace-me
# User-Agent
#AUDIT_EVENTS_TO_WEBHOOK_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#AUDIT_EVENTS_TO_WEBHOOK_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#AUDIT_EVENTS_TO_WEBHOOK_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#AUDIT_EVENTS_TO_WEBHOOK_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#AUDIT_EVENTS_TO_WEBHOOK_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#AUDIT_EVENTS_TO_WEBHOOK_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#AUDIT_EVENTS_TO_WEBHOOK_EXPECT='asset_count>=1'

# --- Automox (automox) ------------------------------------------------------
# Automox API URL
#AUTOMOX_URL=https://console.automox.com
# Organization hint
#AUTOMOX_ORGANIZATION_HINT=replace-me
# Automox API token (required)
#AUTOMOX_API_TOKEN=replace-me
# User-Agent
#AUTOMOX_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#AUTOMOX_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#AUTOMOX_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#AUTOMOX_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#AUTOMOX_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#AUTOMOX_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#AUTOMOX_EXPECT='asset_count>=1'

# --- HCL BigFix (bigfix) ----------------------------------------------------
# BigFix root server URL (required)
#BIGFIX_URL=https://bes.example.com:52311
# Console username (required)
#BIGFIX_USERNAME=replace-me
# Console password (required)
#BIGFIX_PASSWORD=replace-me
# Retrieved properties
#BIGFIX_PROPERTIES=MAC Addresses,Serial Number,Computer Manufacturer,Computer Model,CPU,RAM,BIOS,Device Type,Agent Version,User Name,Active Directory Path,Subnet Address,License Type,Relay
# Installed software property
#BIGFIX_SOFTWARE_PROPERTY=replace-me
# Computers per query
#BIGFIX_PAGE_SIZE=500
# User-Agent
#BIGFIX_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#BIGFIX_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#BIGFIX_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#BIGFIX_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#BIGFIX_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#BIGFIX_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#BIGFIX_EXPECT='asset_count>=1'

# --- Bitdefender GravityZone (bitdefender-gravityzone) ----------------------
# GravityZone Control Center URL (required)
#BITDEFENDER_GRAVITYZONE_URL=https://cloud.gravityzone.bitdefender.com
# API key (required)
#BITDEFENDER_GRAVITYZONE_API_KEY=replace-me
# Parent company or group ID
#BITDEFENDER_GRAVITYZONE_PARENT_ID=replace-me
# Include endpoints without a Bitdefender agent
#BITDEFENDER_GRAVITYZONE_INCLUDE_UNMANAGED=false
# Fetch per-endpoint details
#BITDEFENDER_GRAVITYZONE_FETCH_DETAILS=false
# Per-endpoint detail limit
#BITDEFENDER_GRAVITYZONE_DETAIL_LIMIT=1000
# Page size
#BITDEFENDER_GRAVITYZONE_PAGE_SIZE=100
# User-Agent
#BITDEFENDER_GRAVITYZONE_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#BITDEFENDER_GRAVITYZONE_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#BITDEFENDER_GRAVITYZONE_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#BITDEFENDER_GRAVITYZONE_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#BITDEFENDER_GRAVITYZONE_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#BITDEFENDER_GRAVITYZONE_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#BITDEFENDER_GRAVITYZONE_EXPECT='asset_count>=1'

# --- Bitsight (bitsight) ----------------------------------------------------
# Bitsight API URL
#BITSIGHT_URL=https://api.bitsighttech.com
# Company ID (required)
#BITSIGHT_COMPANY_ID=replace-me
# API token (required)
#BITSIGHT_API_TOKEN=replace-me
# User-Agent
#BITSIGHT_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#BITSIGHT_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#BITSIGHT_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#BITSIGHT_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#BITSIGHT_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#BITSIGHT_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#BITSIGHT_EXPECT='asset_count>=1'

# --- BMC Helix Discovery (bmc-discovery) ------------------------------------
# BMC Discovery appliance URL (required)
#BMC_DISCOVERY_URL=https://discovery.example.com
# REST API version
#BMC_DISCOVERY_API_VERSION=v1.3
# Host filter
#BMC_DISCOVERY_HOST_FILTER=replace-me
# API token (required)
#BMC_DISCOVERY_API_TOKEN=replace-me
# Import software and listening ports
#BMC_DISCOVERY_INCLUDE_DETAILS=true
# Detail enrichment limit
#BMC_DISCOVERY_DETAIL_LIMIT=500
# User-Agent
#BMC_DISCOVERY_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#BMC_DISCOVERY_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#BMC_DISCOVERY_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#BMC_DISCOVERY_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#BMC_DISCOVERY_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#BMC_DISCOVERY_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#BMC_DISCOVERY_EXPECT='asset_count>=1'

# --- Carbon Black (carbon-black) --------------------------------------------
# Carbon Black base URL (required)
#CARBON_BLACK_URL=https://defense.conferdeploy.net
# Organization key (required)
#CARBON_BLACK_ORGANIZATION_KEY=replace-me
# API key (required)
#CARBON_BLACK_API_KEY=replace-me
# User-Agent
#CARBON_BLACK_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#CARBON_BLACK_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#CARBON_BLACK_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#CARBON_BLACK_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#CARBON_BLACK_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#CARBON_BLACK_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#CARBON_BLACK_EXPECT='asset_count>=1'

# --- Checkmk Raw Edition (checkmk-raw) --------------------------------------
# Checkmk URL (required)
#CHECKMK_RAW_URL=https://checkmk.example.com
# Site name (required)
#CHECKMK_RAW_SITE=mysite
# Automation user (required)
#CHECKMK_RAW_USERNAME=automation
# Automation secret (required)
#CHECKMK_RAW_PASSWORD=replace-me
# Collect HW/SW inventory
#CHECKMK_RAW_COLLECT_INVENTORY=true
# Import installed software
#CHECKMK_RAW_COLLECT_SOFTWARE=true
# Livestatus filter
#CHECKMK_RAW_QUERY={"op": "~", "left": "name", "right": "^prod-"}
# Assets per batch
#CHECKMK_RAW_BATCH_SIZE=200
# User-Agent
#CHECKMK_RAW_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#CHECKMK_RAW_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#CHECKMK_RAW_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#CHECKMK_RAW_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#CHECKMK_RAW_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#CHECKMK_RAW_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#CHECKMK_RAW_EXPECT='asset_count>=1'

# --- Cisco ISE (cisco-ise) --------------------------------------------------
# Cisco ISE URL (required)
#CISCO_ISE_URL=https://ise.example.com
# Base64 basic-auth credential (required)
#CISCO_ISE_BASIC_AUTH_CREDENTIAL=replace-me
# User-Agent
#CISCO_ISE_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#CISCO_ISE_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#CISCO_ISE_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#CISCO_ISE_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#CISCO_ISE_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#CISCO_ISE_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#CISCO_ISE_EXPECT='asset_count>=1'

# --- Cisco Secure Endpoint (cisco-secure-endpoint) --------------------------
# Secure Endpoint API host (required)
#CISCO_SECURE_ENDPOINT_API_HOST=https://api.amp.cisco.com
# 3rd Party API Client ID (required)
#CISCO_SECURE_ENDPOINT_CLIENT_ID=replace-me
# API key (required)
#CISCO_SECURE_ENDPOINT_API_KEY=replace-me
# Import vulnerable application findings
#CISCO_SECURE_ENDPOINT_IMPORT_VULNERABILITIES=false
# Vulnerability enrichment limit
#CISCO_SECURE_ENDPOINT_VULNERABILITY_LIMIT=1000
# Only import active connectors
#CISCO_SECURE_ENDPOINT_ACTIVE_ONLY=false
# Include demo computers
#CISCO_SECURE_ENDPOINT_INCLUDE_DEMO=false
# Page size
#CISCO_SECURE_ENDPOINT_PAGE_SIZE=500
# User-Agent
#CISCO_SECURE_ENDPOINT_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#CISCO_SECURE_ENDPOINT_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#CISCO_SECURE_ENDPOINT_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#CISCO_SECURE_ENDPOINT_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#CISCO_SECURE_ENDPOINT_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#CISCO_SECURE_ENDPOINT_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#CISCO_SECURE_ENDPOINT_EXPECT='asset_count>=1'

# --- Cortex XDR (cortex-xdr) ------------------------------------------------
# Cortex XDR base URL (required)
#CORTEX_XDR_URL=https://api-<tenant>.xdr.us.paloaltonetworks.com
# API key ID (required)
#CORTEX_XDR_API_KEY_ID=replace-me
# API key (required)
#CORTEX_XDR_API_KEY=replace-me
# User-Agent
#CORTEX_XDR_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#CORTEX_XDR_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#CORTEX_XDR_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#CORTEX_XDR_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#CORTEX_XDR_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#CORTEX_XDR_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#CORTEX_XDR_EXPECT='asset_count>=1'

# --- Cybereason (cybereason) ------------------------------------------------
# Cybereason URL (required)
#CYBEREASON_URL=https://tenant.cybereason.net:443
# Username (required)
#CYBEREASON_USERNAME=replace-me
# Password (required)
#CYBEREASON_PASSWORD=replace-me
# Import inbound network connections as services
#CYBEREASON_IMPORT_CONNECTIONS=false
# Connection enrichment limit
#CYBEREASON_CONNECTION_LIMIT=500
# Sensors per page
#CYBEREASON_PAGE_SIZE=200
# User-Agent
#CYBEREASON_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#CYBEREASON_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#CYBEREASON_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#CYBEREASON_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#CYBEREASON_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#CYBEREASON_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#CYBEREASON_EXPECT='asset_count>=1'

# --- Cyberint (cyberint) ----------------------------------------------------
# Cyberint base URL (required)
#CYBERINT_URL=https://<tenant>.cyberint.io
# Access token (cookie) (required)
#CYBERINT_ACCESS_TOKEN=replace-me
# User-Agent
#CYBERINT_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#CYBERINT_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#CYBERINT_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#CYBERINT_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#CYBERINT_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#CYBERINT_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#CYBERINT_EXPECT='asset_count>=1'

# --- Cyberwatch (cyberwatch) ------------------------------------------------
# Cyberwatch master scanner URL (required)
#CYBERWATCH_URL=https://192.168.0.1
# API access key (required)
#CYBERWATCH_API_ACCESS_KEY=replace-me
# API secret key (required)
#CYBERWATCH_API_SECRET_KEY=replace-me
# Import software, services, and CVEs
#CYBERWATCH_INCLUDE_DETAILS=true
# Detail enrichment limit
#CYBERWATCH_DETAIL_LIMIT=1000
# Asset category filter
#CYBERWATCH_CATEGORY=replace-me
# User-Agent
#CYBERWATCH_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#CYBERWATCH_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#CYBERWATCH_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#CYBERWATCH_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#CYBERWATCH_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#CYBERWATCH_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#CYBERWATCH_EXPECT='asset_count>=1'

# --- Device42 (device42) ----------------------------------------------------
# Device42 API URL (required)
#DEVICE42_URL=https://device42.example.com
# Auth scheme (required)
#DEVICE42_AUTH_SCHEME=basic
# Credential (required)
#DEVICE42_CREDENTIAL=replace-me
# User-Agent
#DEVICE42_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#DEVICE42_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#DEVICE42_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#DEVICE42_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#DEVICE42_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#DEVICE42_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#DEVICE42_EXPECT='asset_count>=1'

# --- Digital Ocean (digital-ocean) ------------------------------------------
# DigitalOcean API URL
#DIGITAL_OCEAN_URL=https://api.digitalocean.com
# API token (required)
#DIGITAL_OCEAN_API_TOKEN=replace-me
# User-Agent
#DIGITAL_OCEAN_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#DIGITAL_OCEAN_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#DIGITAL_OCEAN_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#DIGITAL_OCEAN_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#DIGITAL_OCEAN_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#DIGITAL_OCEAN_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#DIGITAL_OCEAN_EXPECT='asset_count>=1'

# --- Drata (drata) ----------------------------------------------------------
# Drata API URL
#DRATA_URL=https://public-api.drata.com
# API token (required)
#DRATA_API_TOKEN=replace-me
# User-Agent
#DRATA_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#DRATA_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#DRATA_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#DRATA_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#DRATA_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#DRATA_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#DRATA_EXPECT='asset_count>=1'

# --- exe.dev (exe-dev) ------------------------------------------------------
# exe.dev API URL
#EXE_DEV_URL=https://exe.dev/exec
# API token (required)
#EXE_DEV_API_TOKEN=replace-me
# User-Agent
#EXE_DEV_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#EXE_DEV_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#EXE_DEV_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#EXE_DEV_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#EXE_DEV_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#EXE_DEV_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#EXE_DEV_EXPECT='asset_count>=1'

# --- ExtraHop Reveal(x) (extrahop) ------------------------------------------
# ExtraHop URL (required)
#EXTRAHOP_URL=https://sensor.example.com
# Deployment type (required)
#EXTRAHOP_DEPLOYMENT=enterprise
# RevealX 360 client ID
#EXTRAHOP_CLIENT_ID=replace-me
# API key
#EXTRAHOP_API_KEY=replace-me
# RevealX 360 client secret
#EXTRAHOP_CLIENT_SECRET=replace-me
# Lookback window (days)
#EXTRAHOP_LOOKBACK_DAYS=0
# Only import devices with an IP address
#EXTRAHOP_REQUIRE_IP_ADDRESS=true
# Import observed software
#EXTRAHOP_INCLUDE_SOFTWARE=false
# Devices per request
#EXTRAHOP_PAGE_SIZE=1000
# User-Agent
#EXTRAHOP_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#EXTRAHOP_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#EXTRAHOP_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#EXTRAHOP_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#EXTRAHOP_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#EXTRAHOP_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#EXTRAHOP_EXPECT='asset_count>=1'

# --- Extreme Networks CloudIQ (extreme-cloud-iq) ----------------------------
# Extreme CloudIQ API URL
#EXTREME_CLOUD_IQ_URL=https://api.extremecloudiq.com
# Username (required)
#EXTREME_CLOUD_IQ_USERNAME=replace-me
# Password (required)
#EXTREME_CLOUD_IQ_PASSWORD=replace-me
# User-Agent
#EXTREME_CLOUD_IQ_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#EXTREME_CLOUD_IQ_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#EXTREME_CLOUD_IQ_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#EXTREME_CLOUD_IQ_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#EXTREME_CLOUD_IQ_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#EXTREME_CLOUD_IQ_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#EXTREME_CLOUD_IQ_EXPECT='asset_count>=1'

# --- Fleet (osquery) (fleet-osquery) ----------------------------------------
# Fleet server URL (required)
#FLEET_OSQUERY_URL=https://fleet.example.com
# Fleet user email
#FLEET_OSQUERY_EMAIL=replace-me
# API token
#FLEET_OSQUERY_API_TOKEN=replace-me
# Fleet user password
#FLEET_OSQUERY_PASSWORD=replace-me
# Import software inventory
#FLEET_OSQUERY_INCLUDE_SOFTWARE=true
# Import CVE findings
#FLEET_OSQUERY_INCLUDE_VULNERABILITIES=false
# Import label and team membership
#FLEET_OSQUERY_INCLUDE_LABELS=true
# Import policy results
#FLEET_OSQUERY_INCLUDE_POLICIES=true
# Listening ports additional query name
#FLEET_OSQUERY_LISTENING_PORTS_QUERY=replace-me
# Fetch per-host detail
#FLEET_OSQUERY_INCLUDE_HOST_DETAIL=false
# Per-host detail limit
#FLEET_OSQUERY_DETAIL_LIMIT=500
# Hosts per page
#FLEET_OSQUERY_PAGE_SIZE=100
# User-Agent
#FLEET_OSQUERY_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#FLEET_OSQUERY_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#FLEET_OSQUERY_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#FLEET_OSQUERY_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#FLEET_OSQUERY_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#FLEET_OSQUERY_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#FLEET_OSQUERY_EXPECT='asset_count>=1'

# --- Foreman (foreman) ------------------------------------------------------
# Foreman URL (required)
#FOREMAN_URL=https://foreman.example.com
# Username (required)
#FOREMAN_USERNAME=replace-me
# Password or Personal Access Token (required)
#FOREMAN_PASSWORD=replace-me
# Host search filter
#FOREMAN_SEARCH=replace-me
# Import Facter facts
#FOREMAN_INCLUDE_FACTS=true
# Fetch the interface list per host
#FOREMAN_INCLUDE_INTERFACES=false
# Import Katello package inventory
#FOREMAN_INCLUDE_PACKAGES=false
# Per-host request limit
#FOREMAN_DETAIL_LIMIT=1000
# Hosts per page
#FOREMAN_PAGE_SIZE=100
# Fact values per page
#FOREMAN_FACT_PAGE_SIZE=1000
# Additional fact names
#FOREMAN_EXTRA_FACTS=replace-me
# User-Agent
#FOREMAN_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#FOREMAN_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#FOREMAN_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#FOREMAN_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#FOREMAN_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#FOREMAN_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#FOREMAN_EXPECT='asset_count>=1'

# --- Forescout CounterACT (forescout-counteract) ----------------------------
# Enterprise Manager URL (required)
#FORESCOUT_COUNTERACT_URL=https://enterprise-manager.example.com
# Web API username (required)
#FORESCOUT_COUNTERACT_USERNAME=replace-me
# Web API password (required)
#FORESCOUT_COUNTERACT_PASSWORD=replace-me
# Policy or rule IDs
#FORESCOUT_COUNTERACT_MATCH_RULE_IDS=replace-me
# Host property filter
#FORESCOUT_COUNTERACT_HOST_FILTER=online=true&onsite=true
# Import classification, services, and software
#FORESCOUT_COUNTERACT_INCLUDE_DETAILS=true
# Detail enrichment limit
#FORESCOUT_COUNTERACT_DETAIL_LIMIT=1000
# User-Agent
#FORESCOUT_COUNTERACT_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#FORESCOUT_COUNTERACT_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#FORESCOUT_COUNTERACT_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#FORESCOUT_COUNTERACT_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#FORESCOUT_COUNTERACT_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#FORESCOUT_COUNTERACT_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#FORESCOUT_COUNTERACT_EXPECT='asset_count>=1'

# --- Forescout eyeInspect (forescout-eyeinspect) ----------------------------
# eyeInspect Command Center URL (required)
#FORESCOUT_EYEINSPECT_URL=https://eyeinspect.example.com
# Username (required)
#FORESCOUT_EYEINSPECT_USERNAME=replace-me
# Password (required)
#FORESCOUT_EYEINSPECT_PASSWORD=replace-me
# Lookback window (days)
#FORESCOUT_EYEINSPECT_LAST_SEEN_DAYS=0
# Import alerts as vulnerabilities
#FORESCOUT_EYEINSPECT_INCLUDE_ALERTS=true
# Enrich CVEs from eyeInspect
#FORESCOUT_EYEINSPECT_INCLUDE_CVE_DETAILS=false
# User-Agent
#FORESCOUT_EYEINSPECT_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#FORESCOUT_EYEINSPECT_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#FORESCOUT_EYEINSPECT_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#FORESCOUT_EYEINSPECT_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#FORESCOUT_EYEINSPECT_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#FORESCOUT_EYEINSPECT_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#FORESCOUT_EYEINSPECT_EXPECT='asset_count>=1'

# --- Frontline VM (frontline-vm) --------------------------------------------
# Frontline VM URL (required)
#FRONTLINE_VM_URL=https://vm.frontline.cloud
# API token (required)
#FRONTLINE_VM_API_TOKEN=replace-me
# Import vulnerabilities
#FRONTLINE_VM_INCLUDE_VULNERABILITIES=true
# Minimum vulnerability severity
#FRONTLINE_VM_MIN_VULN_SEVERITY=critical
# Maximum days since scan
#FRONTLINE_VM_MAX_DAYS_SINCE_SCAN=0
# User-Agent
#FRONTLINE_VM_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#FRONTLINE_VM_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#FRONTLINE_VM_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#FRONTLINE_VM_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#FRONTLINE_VM_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#FRONTLINE_VM_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#FRONTLINE_VM_EXPECT='asset_count>=1'

# --- Ghost Security (ghost) -------------------------------------------------
# Ghost Security API URL
#GHOST_URL=https://api.ghostsecurity.ai
# API token (required)
#GHOST_API_TOKEN=replace-me
# User-Agent
#GHOST_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#GHOST_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#GHOST_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#GHOST_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#GHOST_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#GHOST_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#GHOST_EXPECT='asset_count>=1'

# --- GLPI (glpi) ------------------------------------------------------------
# GLPI URL (required)
#GLPI_URL=https://glpi.example.com
# Username
#GLPI_USERNAME=replace-me
# Application token
#GLPI_APP_TOKEN=replace-me
# User API token
#GLPI_USER_TOKEN=replace-me
# Password
#GLPI_PASSWORD=replace-me
# Import installed software
#GLPI_INCLUDE_SOFTWARE=true
# Software enrichment limit
#GLPI_DETAIL_LIMIT=1000
# Import network equipment and printers
#GLPI_INCLUDE_NETWORK_DEVICES=false
# User-Agent
#GLPI_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#GLPI_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#GLPI_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#GLPI_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#GLPI_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#GLPI_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#GLPI_EXPECT='asset_count>=1'

# --- Halcyon ----------------------------------------------------------------
# Halcyon API URL
#HALCYON_URL=https://api.halcyon.ai
# Username (required)
#HALCYON_USERNAME=replace-me
# Password (required)
#HALCYON_PASSWORD=replace-me
# User-Agent
#HALCYON_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#HALCYON_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#HALCYON_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#HALCYON_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#HALCYON_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#HALCYON_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#HALCYON_EXPECT='asset_count>=1'

# --- Home Assistant (home-assistant) ----------------------------------------
# Home Assistant URL (required)
#HOME_ASSISTANT_URL=https://homeassistant.example.com:8123
# Long-lived access token (required)
#HOME_ASSISTANT_API_TOKEN=replace-me
# Collect the device registry
#HOME_ASSISTANT_COLLECT_DEVICE_REGISTRY=true
# Collect device trackers
#HOME_ASSISTANT_COLLECT_DEVICE_TRACKERS=true
# Include absent trackers
#HOME_ASSISTANT_INCLUDE_AWAY=true
# Mine addresses from all entities
#HOME_ASSISTANT_MINE_ENTITY_ADDRESSES=false
# Devices per template request
#HOME_ASSISTANT_DEVICE_PAGE_SIZE=100
# User-Agent
#HOME_ASSISTANT_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#HOME_ASSISTANT_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#HOME_ASSISTANT_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#HOME_ASSISTANT_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#HOME_ASSISTANT_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#HOME_ASSISTANT_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#HOME_ASSISTANT_EXPECT='asset_count>=1'

# --- Icinga 2 (icinga2) -----------------------------------------------------
# Icinga 2 API URL (required)
#ICINGA2_URL=https://icinga.example.com:5665
# API user (required)
#ICINGA2_USERNAME=replace-me
# API password (required)
#ICINGA2_PASSWORD=replace-me
# Host group
#ICINGA2_HOST_GROUP=replace-me
# Host filter expression
#ICINGA2_HOST_FILTER=host.vars.site == "dc1"
# Import last check output
#ICINGA2_INCLUDE_CHECK_OUTPUT=false
# Summarize service checks
#ICINGA2_INCLUDE_CHECKS=false
# Check names per host
#ICINGA2_MAX_CHECK_NAMES=40
# User-Agent
#ICINGA2_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#ICINGA2_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#ICINGA2_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#ICINGA2_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#ICINGA2_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#ICINGA2_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#ICINGA2_EXPECT='asset_count>=1'

# --- Illumio Core (illumio-core) --------------------------------------------
# PCE URL (required)
#ILLUMIO_CORE_URL=https://pce.example.com:8443
# Organization ID (required)
#ILLUMIO_CORE_ORG_ID=1
# API key username (required)
#ILLUMIO_CORE_API_KEY_ID=api_1a2b3c4d5e6f7890
# API key secret (required)
#ILLUMIO_CORE_API_SECRET=replace-me
# Import listening services
#ILLUMIO_CORE_IMPORT_SERVICES=true
# Service enrichment limit
#ILLUMIO_CORE_SERVICE_DETAIL_LIMIT=1000
# Only import managed workloads
#ILLUMIO_CORE_MANAGED_ONLY=false
# Asynchronous collection timeout (seconds)
#ILLUMIO_CORE_ASYNC_TIMEOUT_SECONDS=600
# User-Agent
#ILLUMIO_CORE_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#ILLUMIO_CORE_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#ILLUMIO_CORE_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#ILLUMIO_CORE_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#ILLUMIO_CORE_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#ILLUMIO_CORE_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#ILLUMIO_CORE_EXPECT='asset_count>=1'

# --- Infoblox NIOS (infoblox) -----------------------------------------------
# Grid Manager URL (required)
#INFOBLOX_URL=https://gridmaster.example.com
# WAPI version
#INFOBLOX_WAPI_VERSION=v2.13.1
# Username (required)
#INFOBLOX_USERNAME=replace-me
# Password (required)
#INFOBLOX_PASSWORD=replace-me
# Network view
#INFOBLOX_NETWORK_VIEW=replace-me
# Import DHCP leases
#INFOBLOX_INCLUDE_LEASES=true
# Import host records
#INFOBLOX_INCLUDE_HOST_RECORDS=false
# Import unused addresses
#INFOBLOX_INCLUDE_UNUSED_ADDRESSES=false
# Page size
#INFOBLOX_PAGE_SIZE=1000
# User-Agent
#INFOBLOX_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#INFOBLOX_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#INFOBLOX_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#INFOBLOX_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#INFOBLOX_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#INFOBLOX_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#INFOBLOX_EXPECT='asset_count>=1'

# --- iTop (itop) ------------------------------------------------------------
# iTop URL (required)
#ITOP_URL=https://itop.example.com
# Username
#ITOP_USERNAME=replace-me
# Password
#ITOP_PASSWORD=replace-me
# Authentication token
#ITOP_AUTH_TOKEN=replace-me
# CI classes
#ITOP_CLASSES=Server,VirtualMachine,NetworkDevice,PC
# OQL filter
#ITOP_OQL_FILTER=status != 'obsolete'
# REST API version
#ITOP_API_VERSION=1.4
# Page size
#ITOP_PAGE_SIZE=200
# Import network interfaces
#ITOP_INCLUDE_INTERFACES=true
# User-Agent
#ITOP_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#ITOP_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#ITOP_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#ITOP_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#ITOP_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#ITOP_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#ITOP_EXPECT='asset_count>=1'

# --- Ivanti Neurons (ivanti_neurons) ----------------------------------------
# Ivanti Neurons URL (required)
#IVANTI_NEURONS_URL=https://nvuprd-sfc.ivanticloud.com
# Tenant ID (required)
#IVANTI_NEURONS_TENANT_ID=replace-me
# OAuth client ID (required)
#IVANTI_NEURONS_CLIENT_ID=replace-me
# OAuth client secret (required)
#IVANTI_NEURONS_CLIENT_SECRET=replace-me
# User-Agent
#IVANTI_NEURONS_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#IVANTI_NEURONS_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#IVANTI_NEURONS_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#IVANTI_NEURONS_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#IVANTI_NEURONS_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#IVANTI_NEURONS_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#IVANTI_NEURONS_EXPECT='asset_count>=1'

# --- JAMF (jamf) ------------------------------------------------------------
# Jamf base URL (required)
#JAMF_URL=https://<tenant>.jamfcloud.com
# API client ID (required)
#JAMF_CLIENT_ID=replace-me
# API client secret (required)
#JAMF_CLIENT_SECRET=replace-me
# User-Agent
#JAMF_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#JAMF_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#JAMF_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#JAMF_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#JAMF_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#JAMF_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#JAMF_EXPECT='asset_count>=1'

# --- JumpCloud (jumpcloud) --------------------------------------------------
# JumpCloud console URL
#JUMPCLOUD_URL=https://vendor.example.com
# Organization ID
#JUMPCLOUD_ORG_ID=replace-me
# API key (required)
#JUMPCLOUD_API_KEY=replace-me
# Import bound users
#JUMPCLOUD_INCLUDE_BOUND_USERS=true
# Import installed software
#JUMPCLOUD_INCLUDE_SOFTWARE=false
# Import MAC addresses
#JUMPCLOUD_INCLUDE_MAC_ADDRESSES=false
# Per-system detail limit
#JUMPCLOUD_DETAIL_SYSTEM_LIMIT=1000
# Page size
#JUMPCLOUD_PAGE_SIZE=100
# User-Agent
#JUMPCLOUD_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#JUMPCLOUD_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#JUMPCLOUD_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#JUMPCLOUD_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#JUMPCLOUD_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#JUMPCLOUD_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#JUMPCLOUD_EXPECT='asset_count>=1'

# --- Kandji (kandji) --------------------------------------------------------
# Kandji API URL (required)
#KANDJI_URL=https://<subdomain>.api.kandji.io/api/v1
# API token (required)
#KANDJI_API_TOKEN=replace-me
# User-Agent
#KANDJI_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#KANDJI_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#KANDJI_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#KANDJI_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#KANDJI_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#KANDJI_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#KANDJI_EXPECT='asset_count>=1'

# --- Kenna Security (kenna) -------------------------------------------------
# Kenna API URL (required)
#KENNA_URL=https://api.kennasecurity.com
# API token (required)
#KENNA_API_TOKEN=replace-me
# Import vulnerabilities
#KENNA_INCLUDE_VULNERABILITIES=true
# Asset status
#KENNA_ASSET_STATUS=active
# Minimum asset risk meter score
#KENNA_MIN_RISK_METER_SCORE=0
# Import non-network assets
#KENNA_INCLUDE_NON_NETWORK_ASSETS=false
# Page size
#KENNA_PAGE_SIZE=500
# User-Agent
#KENNA_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#KENNA_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#KENNA_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#KENNA_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#KENNA_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#KENNA_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#KENNA_EXPECT='asset_count>=1'

# --- Kubernetes (kubernetes) ------------------------------------------------
# API server URL (required)
#KUBERNETES_URL=https://k8s.example.com:6443
# Bearer token (required)
#KUBERNETES_BEARER_TOKEN=replace-me
# Import LoadBalancer/NodePort services
#KUBERNETES_INCLUDE_LOADBALANCER_SERVICES=true
# Request timeout (seconds)
#KUBERNETES_REQUEST_TIMEOUT=300
# User-Agent
#KUBERNETES_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#KUBERNETES_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#KUBERNETES_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#KUBERNETES_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#KUBERNETES_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#KUBERNETES_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#KUBERNETES_EXPECT='asset_count>=1'

# --- Lansweeper (lansweeper) ------------------------------------------------
# Lansweeper API URL
#LANSWEEPER_URL=https://vendor.example.com
# Site IDs
#LANSWEEPER_SITE_IDS=replace-me
# Application identity code (required)
#LANSWEEPER_APPLICATION_IDENTITY_CODE=replace-me
# Import installed software
#LANSWEEPER_IMPORT_SOFTWARE=true
# Assets per page
#LANSWEEPER_PAGE_SIZE=200
# User-Agent
#LANSWEEPER_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#LANSWEEPER_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#LANSWEEPER_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#LANSWEEPER_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#LANSWEEPER_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#LANSWEEPER_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#LANSWEEPER_EXPECT='asset_count>=1'

# --- LibreNMS (librenms) ----------------------------------------------------
# LibreNMS URL (required)
#LIBRENMS_URL=https://librenms.example.com
# API token (required)
#LIBRENMS_API_TOKEN=replace-me
# Device filter
#LIBRENMS_DEVICE_FILTER=replace-me
# Collect device IP addresses
#LIBRENMS_COLLECT_ADDRESSES=true
# Collect device ports
#LIBRENMS_COLLECT_PORTS=true
# Collect the global ARP table
#LIBRENMS_COLLECT_ARP=true
# Collect the global MAC forwarding table
#LIBRENMS_COLLECT_FDB=false
# Port columns
#LIBRENMS_PORT_COLUMNS=port_id,ifIndex,ifName,ifDescr,ifAlias,ifPhysAddress,ifOperStatus,ifAdminStatus,ifType,ifSpeed
# Maximum devices
#LIBRENMS_MAX_DEVICES=5000
# Maximum discovered endpoints
#LIBRENMS_MAX_DISCOVERED=50000
# User-Agent
#LIBRENMS_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#LIBRENMS_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#LIBRENMS_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#LIBRENMS_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#LIBRENMS_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#LIBRENMS_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#LIBRENMS_EXPECT='asset_count>=1'

# --- LimaCharlie (lima-charlie) ---------------------------------------------
# LimaCharlie API URL
#LIMA_CHARLIE_URL=https://api.limacharlie.io/v1
# LimaCharlie JWT URL
#LIMA_CHARLIE_JWT_URL=https://jwt.limacharlie.io
# Organization ID (OID) (required)
#LIMA_CHARLIE_ORGANIZATION_ID=replace-me
# API access token (required)
#LIMA_CHARLIE_API_TOKEN=replace-me
# User-Agent
#LIMA_CHARLIE_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#LIMA_CHARLIE_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#LIMA_CHARLIE_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#LIMA_CHARLIE_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#LIMA_CHARLIE_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#LIMA_CHARLIE_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#LIMA_CHARLIE_EXPECT='asset_count>=1'

# --- Linux via SSH (linux-ssh) ----------------------------------------------
# Target host (required)
#LINUX_SSH_HOST=replace-me
# SSH port
#LINUX_SSH_PORT=22
# Username (required)
#LINUX_SSH_USERNAME=replace-me
# Password
#LINUX_SSH_PASSWORD=replace-me
# Private key (PEM)
#LINUX_SSH_PRIVATE_KEY=replace-me
# Private key passphrase
#LINUX_SSH_PRIVATE_KEY_PASSPHRASE=replace-me
# Expected host public key (authorized_keys format)
#LINUX_SSH_HOST_KEY=replace-me
# Connection timeout (seconds)
#LINUX_SSH_TIMEOUT=30
# What a valid result looks like (required to run; see tests/README.md)
#LINUX_SSH_EXPECT='asset_count>=1'

# --- ManageEngine Endpoint Central (manage-engine-endpoint-central) ---------
# Endpoint Central URL (required)
#MANAGE_ENGINE_ENDPOINT_CENTRAL_URL=https://ec.example.com
# OAuth token (required)
#MANAGE_ENGINE_ENDPOINT_CENTRAL_OAUTH_TOKEN=replace-me
# User-Agent
#MANAGE_ENGINE_ENDPOINT_CENTRAL_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#MANAGE_ENGINE_ENDPOINT_CENTRAL_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#MANAGE_ENGINE_ENDPOINT_CENTRAL_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#MANAGE_ENGINE_ENDPOINT_CENTRAL_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#MANAGE_ENGINE_ENDPOINT_CENTRAL_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#MANAGE_ENGINE_ENDPOINT_CENTRAL_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#MANAGE_ENGINE_ENDPOINT_CENTRAL_EXPECT='asset_count>=1'

# --- Maze (maze-security) ---------------------------------------------------
# Maze API URL
#MAZE_SECURITY_URL=https://api.mazehq.com
# Maze API key (required)
#MAZE_SECURITY_API_KEY=replace-me
# Lookback window (days)
#MAZE_SECURITY_DAYS_BACK=30
# User-Agent
#MAZE_SECURITY_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#MAZE_SECURITY_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#MAZE_SECURITY_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#MAZE_SECURITY_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#MAZE_SECURITY_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#MAZE_SECURITY_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#MAZE_SECURITY_EXPECT='asset_count>=1'

# --- MikroTik RouterOS (mikrotik-routeros) ----------------------------------
# RouterOS URL (required)
#MIKROTIK_ROUTEROS_URL=https://192.168.88.1
# Username (required)
#MIKROTIK_ROUTEROS_USERNAME=replace-me
# Password (required)
#MIKROTIK_ROUTEROS_PASSWORD=replace-me
# Import the router itself
#MIKROTIK_ROUTEROS_IMPORT_ROUTER=true
# Collect ARP entries
#MIKROTIK_ROUTEROS_COLLECT_ARP=true
# Collect DHCP leases
#MIKROTIK_ROUTEROS_COLLECT_DHCP_LEASES=true
# Collect discovery neighbors
#MIKROTIK_ROUTEROS_COLLECT_NEIGHBORS=true
# Collect wireless stations
#MIKROTIK_ROUTEROS_COLLECT_WIRELESS=true
# Bound DHCP leases only
#MIKROTIK_ROUTEROS_BOUND_LEASES_ONLY=false
# Maximum hosts
#MIKROTIK_ROUTEROS_MAX_HOSTS=10000
# User-Agent
#MIKROTIK_ROUTEROS_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#MIKROTIK_ROUTEROS_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#MIKROTIK_ROUTEROS_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#MIKROTIK_ROUTEROS_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#MIKROTIK_ROUTEROS_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#MIKROTIK_ROUTEROS_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#MIKROTIK_ROUTEROS_EXPECT='asset_count>=1'

# --- Mosyle ------------------------------------------------------------------
# Mosyle API URL
#MOSYLE_URL=https://managerapi.mosyle.com/v2
# API token (required)
#MOSYLE_API_TOKEN=replace-me
# Account email
#MOSYLE_EMAIL=replace-me
# Account password
#MOSYLE_PASSWORD=replace-me
# Legacy JSON credential
#MOSYLE_LEGACY_CREDENTIALS=replace-me
# User-Agent
#MOSYLE_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#MOSYLE_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#MOSYLE_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#MOSYLE_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#MOSYLE_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#MOSYLE_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#MOSYLE_EXPECT='asset_count>=1'

# --- Microsoft SQL Server databases (mssql-databases) -----------------------
# SQL Server host (required)
#MSSQL_DATABASES_HOST=replace-me
# Port
#MSSQL_DATABASES_PORT=1433
# Username (required)
#MSSQL_DATABASES_USERNAME=replace-me
# Password (required)
#MSSQL_DATABASES_PASSWORD=replace-me
# Initial catalog
#MSSQL_DATABASES_DATABASE=master
# Require TLS encryption
#MSSQL_DATABASES_ENCRYPT=true
# Query timeout (seconds)
#MSSQL_DATABASES_TIMEOUT=30
# What a valid result looks like (required to run; see tests/README.md)
#MSSQL_DATABASES_EXPECT='asset_count>=1'

# --- Nautobot (nautobot) ----------------------------------------------------
# Nautobot URL (required)
#NAUTOBOT_URL=https://nautobot.example.com
# API token (required)
#NAUTOBOT_API_TOKEN=replace-me
# REST API version
#NAUTOBOT_API_VERSION=2.4
# Import DCIM devices
#NAUTOBOT_IMPORT_DEVICES=true
# Import virtual machines
#NAUTOBOT_IMPORT_VIRTUAL_MACHINES=true
# Collect interfaces
#NAUTOBOT_COLLECT_INTERFACES=true
# Device filter
#NAUTOBOT_DEVICE_FILTER=status=active&role=router
# Virtual machine filter
#NAUTOBOT_VIRTUAL_MACHINE_FILTER=status=active
# Records per page
#NAUTOBOT_PAGE_SIZE=100
# User-Agent
#NAUTOBOT_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#NAUTOBOT_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#NAUTOBOT_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#NAUTOBOT_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#NAUTOBOT_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#NAUTOBOT_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#NAUTOBOT_EXPECT='asset_count>=1'

# --- Netdata (netdata) ------------------------------------------------------
# Netdata Agent URL (required)
#NETDATA_URL=https://netdata-parent.example.com:19999
# Bearer token
#NETDATA_API_TOKEN=replace-me
# API version
#NETDATA_API_VERSION=auto
# Collect per-node system detail
#NETDATA_COLLECT_NODE_DETAILS=true
# Maximum nodes to enrich
#NETDATA_MAX_DETAIL_NODES=500
# Import stale and offline nodes
#NETDATA_INCLUDE_UNREACHABLE_NODES=true
# Attach the configured address to the local agent
#NETDATA_ATTACH_AGENT_ADDRESS=false
# User-Agent
#NETDATA_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#NETDATA_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#NETDATA_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#NETDATA_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#NETDATA_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#NETDATA_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#NETDATA_EXPECT='asset_count>=1'

# --- Netdisco (netdisco) ----------------------------------------------------
# Netdisco URL (required)
#NETDISCO_URL=https://netdisco.example.com
# Username (required)
#NETDISCO_USERNAME=replace-me
# Password or API key (required)
#NETDISCO_PASSWORD=replace-me
# Collect layer-2 nodes
#NETDISCO_COLLECT_NODES=true
# Active nodes only
#NETDISCO_ACTIVE_ONLY=true
# Maximum node age in days
#NETDISCO_MAX_AGE_DAYS=30
# Collect device interface addresses
#NETDISCO_COLLECT_DEVICE_IPS=true
# Collect switchport MACs
#NETDISCO_COLLECT_PORTS=false
# IP inventory subnets
#NETDISCO_IP_INVENTORY_SUBNETS=replace-me
# Maximum devices
#NETDISCO_MAX_DEVICES=5000
# Maximum nodes
#NETDISCO_MAX_NODES=50000
# Device page size
#NETDISCO_PAGE_SIZE=200
# Device fields
#NETDISCO_DEVICE_FIELDS=ip,dns,name,description,location,contact,model,os,os_ver,vendor,serial,chassis_id,layers,uptime,mac,num_ports,snmp_ver,snmp_class,is_pseudo,creation,last_discover,last_macsuck,last_arpnip
# User-Agent
#NETDISCO_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#NETDISCO_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#NETDISCO_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#NETDISCO_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#NETDISCO_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#NETDISCO_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#NETDISCO_EXPECT='asset_count>=1'

# --- Netskope (netskope) ----------------------------------------------------
# Netskope tenant URL (required)
#NETSKOPE_URL=https://<tenant>.goskope.com
# API token (required)
#NETSKOPE_API_TOKEN=replace-me
# User-Agent
#NETSKOPE_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#NETSKOPE_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#NETSKOPE_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#NETSKOPE_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#NETSKOPE_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#NETSKOPE_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#NETSKOPE_EXPECT='asset_count>=1'

# --- Nexthink (nexthink) ----------------------------------------------------
# Nexthink authentication URL (required)
#NEXTHINK_AUTH_URL=https://<instance>-login.<region>.nexthink.cloud
# Nexthink API URL (required)
#NEXTHINK_API_URL=https://<instance>.api.<region>.nexthink.cloud
# Client ID (required)
#NEXTHINK_CLIENT_ID=replace-me
# Client secret (required)
#NEXTHINK_CLIENT_SECRET=replace-me
# NQL query ID
#NEXTHINK_QUERY_ID=#runzero_integration
# OAuth scope
#NEXTHINK_SCOPE=service:integration
# User-Agent
#NEXTHINK_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#NEXTHINK_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#NEXTHINK_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#NEXTHINK_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#NEXTHINK_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#NEXTHINK_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#NEXTHINK_EXPECT='asset_count>=1'

# --- NinjaOne (ninjaone) ----------------------------------------------------
# NinjaOne API URL (required)
#NINJAONE_API_URL=https://us2.ninjarmm.com
# OAuth client ID (required)
#NINJAONE_CLIENT_ID=replace-me
# OAuth client secret (required)
#NINJAONE_CLIENT_SECRET=replace-me
# User-Agent
#NINJAONE_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#NINJAONE_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#NINJAONE_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#NINJAONE_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#NINJAONE_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#NINJAONE_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#NINJAONE_EXPECT='asset_count>=1'

# --- Nozomi Networks (nozomi-networks) --------------------------------------
# Nozomi Networks URL (required)
#NOZOMI_NETWORKS_URL=https://guardian.example.com
# API key name (required)
#NOZOMI_NETWORKS_KEY_NAME=replace-me
# API key token (required)
#NOZOMI_NETWORKS_KEY_TOKEN=replace-me
# Asset query filter
#NOZOMI_NETWORKS_ASSET_FILTER=where level == 4
# Import CVE findings
#NOZOMI_NETWORKS_INCLUDE_CVES=true
# User-Agent
#NOZOMI_NETWORKS_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#NOZOMI_NETWORKS_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#NOZOMI_NETWORKS_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#NOZOMI_NETWORKS_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#NOZOMI_NETWORKS_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#NOZOMI_NETWORKS_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#NOZOMI_NETWORKS_EXPECT='asset_count>=1'

# --- ntopng (ntopng) --------------------------------------------------------
# ntopng URL (required)
#NTOPNG_URL=http://ntopng.example.com:3000
# Username
#NTOPNG_USERNAME=replace-me
# Password
#NTOPNG_PASSWORD=replace-me
# API token
#NTOPNG_API_TOKEN=replace-me
# Interface ids
#NTOPNG_INTERFACE_IDS=replace-me
# Host selection
#NTOPNG_HOST_MODE=local
# Page size
#NTOPNG_PAGE_SIZE=250
# Maximum hosts per interface
#NTOPNG_MAX_HOSTS=25000
# Join the MAC table
#NTOPNG_INCLUDE_MACS=true
# User-Agent
#NTOPNG_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#NTOPNG_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#NTOPNG_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#NTOPNG_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#NTOPNG_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#NTOPNG_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#NTOPNG_EXPECT='asset_count>=1'

# --- Nutanix Prism (nutanix-prism) ------------------------------------------
# Prism Element URL (required)
#NUTANIX_PRISM_URL=https://prism.example.com:9440
# Username (required)
#NUTANIX_PRISM_USERNAME=replace-me
# Password (required)
#NUTANIX_PRISM_PASSWORD=replace-me
# Import guest VMs
#NUTANIX_PRISM_IMPORT_VMS=true
# Records per page
#NUTANIX_PRISM_PAGE_SIZE=250
# User-Agent
#NUTANIX_PRISM_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#NUTANIX_PRISM_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#NUTANIX_PRISM_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#NUTANIX_PRISM_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#NUTANIX_PRISM_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#NUTANIX_PRISM_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#NUTANIX_PRISM_EXPECT='asset_count>=1'

# --- OCS Inventory NG (ocs-inventory) ---------------------------------------
# OCS Inventory URL (required)
#OCS_INVENTORY_URL=https://ocs.example.com
# Username
#OCS_INVENTORY_USERNAME=replace-me
# Password
#OCS_INVENTORY_PASSWORD=replace-me
# Import installed software
#OCS_INVENTORY_INCLUDE_SOFTWARE=false
# Software enrichment limit
#OCS_INVENTORY_SOFTWARE_LIMIT=1000
# Ignore computers not seen in (days)
#OCS_INVENTORY_STALE_DAYS=0
# User-Agent
#OCS_INVENTORY_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#OCS_INVENTORY_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#OCS_INVENTORY_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#OCS_INVENTORY_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#OCS_INVENTORY_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#OCS_INVENTORY_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#OCS_INVENTORY_EXPECT='asset_count>=1'

# --- Open-AudIT Community (open-audit) --------------------------------------
# Open-AudIT server URL (required)
#OPEN_AUDIT_URL=https://openaudit.example.com
# Application path
#OPEN_AUDIT_APP_PATH=/open-audit
# Username (required)
#OPEN_AUDIT_USERNAME=replace-me
# Password (required)
#OPEN_AUDIT_PASSWORD=replace-me
# Organisation ID
#OPEN_AUDIT_ORG_ID=replace-me
# Include retired devices
#OPEN_AUDIT_INCLUDE_RETIRED=false
# Collect addresses and adapters
#OPEN_AUDIT_COLLECT_INTERFACES=true
# Collect installed software
#OPEN_AUDIT_COLLECT_SOFTWARE=false
# Detail request cap
#OPEN_AUDIT_MAX_DETAIL_DEVICES=2000
# Page size
#OPEN_AUDIT_PAGE_SIZE=500
# User-Agent
#OPEN_AUDIT_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#OPEN_AUDIT_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#OPEN_AUDIT_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#OPEN_AUDIT_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#OPEN_AUDIT_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#OPEN_AUDIT_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#OPEN_AUDIT_EXPECT='asset_count>=1'

# --- OpenNMS Horizon (opennms-horizon) --------------------------------------
# OpenNMS URL (required)
#OPENNMS_HORIZON_URL=https://opennms.example.com:8980
# Username (required)
#OPENNMS_HORIZON_USERNAME=replace-me
# Password (required)
#OPENNMS_HORIZON_PASSWORD=replace-me
# Application context path
#OPENNMS_HORIZON_CONTEXT_PATH=/opennms
# Collect interfaces
#OPENNMS_HORIZON_COLLECT_INTERFACES=true
# Collect SNMP-only interfaces
#OPENNMS_HORIZON_COLLECT_SNMP_INTERFACES=true
# Record monitored services
#OPENNMS_HORIZON_COLLECT_SERVICES=true
# Per-node request limit
#OPENNMS_HORIZON_DETAIL_LIMIT=1000
# Records per page
#OPENNMS_HORIZON_PAGE_SIZE=200
# User-Agent
#OPENNMS_HORIZON_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#OPENNMS_HORIZON_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#OPENNMS_HORIZON_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#OPENNMS_HORIZON_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#OPENNMS_HORIZON_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#OPENNMS_HORIZON_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#OPENNMS_HORIZON_EXPECT='asset_count>=1'

# --- OpenWrt (openwrt) ------------------------------------------------------
# OpenWrt URL (required)
#OPENWRT_URL=https://192.168.1.1
# Username (required)
#OPENWRT_USERNAME=replace-me
# Password (required)
#OPENWRT_PASSWORD=replace-me
# Import the router itself
#OPENWRT_IMPORT_ROUTER=true
# Collect DHCP leases
#OPENWRT_COLLECT_LEASES=true
# Collect host hints
#OPENWRT_COLLECT_HOST_HINTS=true
# Collect wireless stations
#OPENWRT_COLLECT_WIRELESS=true
# Maximum hosts
#OPENWRT_MAX_HOSTS=5000
# Session timeout in seconds
#OPENWRT_SESSION_TIMEOUT=900
# User-Agent
#OPENWRT_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#OPENWRT_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#OPENWRT_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#OPENWRT_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#OPENWRT_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#OPENWRT_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#OPENWRT_EXPECT='asset_count>=1'

# --- OPNsense (opnsense) ----------------------------------------------------
# OPNsense URL (required)
#OPNSENSE_URL=https://opnsense.example.com
# API key (required)
#OPNSENSE_API_KEY=replace-me
# API secret (required)
#OPNSENSE_API_SECRET=replace-me
# Collect the ARP table
#OPNSENSE_COLLECT_ARP=true
# Collect the NDP table
#OPNSENSE_COLLECT_NDP=true
# Collect DHCP leases
#OPNSENSE_COLLECT_LEASES=true
# Include expired neighbors
#OPNSENSE_INCLUDE_EXPIRED=false
# Maximum discovered hosts
#OPNSENSE_MAX_HOSTS=20000
# User-Agent
#OPNSENSE_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#OPNSENSE_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#OPNSENSE_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#OPNSENSE_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#OPNSENSE_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#OPNSENSE_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#OPNSENSE_EXPECT='asset_count>=1'

# --- Palo Alto Networks Device Security (palo-alto-device-security) ---------
# Device Security API URL (required)
#PALO_ALTO_DEVICE_SECURITY_URL=https://api.strata.paloaltonetworks.com
# Strata Cloud Manager auth URL
#PALO_ALTO_DEVICE_SECURITY_AUTH_URL=https://vendor.example.com
# Tenant Service Group ID (required)
#PALO_ALTO_DEVICE_SECURITY_TSG_ID=replace-me
# Service account client ID (required)
#PALO_ALTO_DEVICE_SECURITY_CLIENT_ID=replace-me
# Service account client secret (required)
#PALO_ALTO_DEVICE_SECURITY_CLIENT_SECRET=replace-me
# Lookback window (days)
#PALO_ALTO_DEVICE_SECURITY_LOOKBACK_DAYS=30
# User-Agent
#PALO_ALTO_DEVICE_SECURITY_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#PALO_ALTO_DEVICE_SECURITY_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#PALO_ALTO_DEVICE_SECURITY_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#PALO_ALTO_DEVICE_SECURITY_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#PALO_ALTO_DEVICE_SECURITY_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#PALO_ALTO_DEVICE_SECURITY_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#PALO_ALTO_DEVICE_SECURITY_EXPECT='asset_count>=1'

# --- pfSense (pfsense) ------------------------------------------------------
# pfSense base URL (required)
#PFSENSE_BASE_URL=https://pfsense.example.com
# API token (required)
#PFSENSE_API_TOKEN=replace-me
# Auth header name
#PFSENSE_AUTH_HEADER=X-API-Key
# Legacy JSON credential
#PFSENSE_LEGACY_CREDENTIALS=replace-me
# User-Agent
#PFSENSE_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#PFSENSE_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#PFSENSE_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#PFSENSE_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#PFSENSE_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#PFSENSE_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#PFSENSE_EXPECT='asset_count>=1'

# --- phpIPAM (phpipam) ------------------------------------------------------
# phpIPAM URL (required)
#PHPIPAM_URL=https://ipam.example.com
# API app ID (required)
#PHPIPAM_APP_ID=runzero
# Username (required)
#PHPIPAM_USERNAME=replace-me
# Password (required)
#PHPIPAM_PASSWORD=replace-me
# Import IPAM addresses
#PHPIPAM_IMPORT_ADDRESSES=true
# Import registered devices
#PHPIPAM_IMPORT_DEVICES=true
# Sections
#PHPIPAM_SECTIONS=replace-me
# Address tags
#PHPIPAM_ADDRESS_TAGS=replace-me
# Skip addresses with no hostname or MAC
#PHPIPAM_REQUIRE_IDENTITY=true
# Maximum subnets
#PHPIPAM_MAX_SUBNETS=5000
# Maximum addresses
#PHPIPAM_MAX_ADDRESSES=100000
# User-Agent
#PHPIPAM_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#PHPIPAM_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#PHPIPAM_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#PHPIPAM_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#PHPIPAM_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#PHPIPAM_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#PHPIPAM_EXPECT='asset_count>=1'

# --- Pi-hole (pihole) -------------------------------------------------------
# Pi-hole URL (required)
#PIHOLE_URL=https://pi.hole
# Password or application password (required)
#PIHOLE_PASSWORD=replace-me
# Maximum devices
#PIHOLE_MAX_DEVICES=10000
# Maximum addresses per device
#PIHOLE_MAX_ADDRESSES=32
# Join DHCP leases
#PIHOLE_INCLUDE_LEASES=true
# Import devices with no MAC
#PIHOLE_INCLUDE_IP_ONLY_DEVICES=false
# User-Agent
#PIHOLE_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#PIHOLE_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#PIHOLE_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#PIHOLE_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#PIHOLE_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#PIHOLE_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#PIHOLE_EXPECT='asset_count>=1'

# --- Portainer / Docker Engine (portainer) ----------------------------------
# Connection mode
#PORTAINER_MODE=portainer
# Base URL (required)
#PORTAINER_URL=https://portainer.example.com:9443
# Portainer API key
#PORTAINER_API_KEY=replace-me
# Limit to environment IDs
#PORTAINER_ENVIRONMENT_IDS=replace-me
# Import containers as assets
#PORTAINER_IMPORT_CONTAINERS=routable-only
# Additional routable network names
#PORTAINER_ROUTABLE_NETWORKS=replace-me
# Import published ports as services
#PORTAINER_IMPORT_PUBLISHED_PORTS=true
# Include stopped containers
#PORTAINER_INCLUDE_STOPPED_CONTAINERS=true
# Maximum environments to collect
#PORTAINER_MAX_ENVIRONMENTS=200
# Portainer page size
#PORTAINER_PAGE_SIZE=100
# User-Agent
#PORTAINER_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#PORTAINER_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#PORTAINER_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#PORTAINER_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#PORTAINER_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#PORTAINER_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#PORTAINER_EXPECT='asset_count>=1'

# --- Proxmox (proxmox) ------------------------------------------------------
# Proxmox base URL (required)
#PROXMOX_BASE_URL=https://pve.example.com:8006
# API token ID (required)
#PROXMOX_API_TOKEN_ID=replace-me
# API token secret (required)
#PROXMOX_API_TOKEN_SECRET=replace-me
# User-Agent
#PROXMOX_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#PROXMOX_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#PROXMOX_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#PROXMOX_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#PROXMOX_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#PROXMOX_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#PROXMOX_EXPECT='asset_count>=1'

# --- PuppetDB (puppetdb) ----------------------------------------------------
# PuppetDB URL (required)
#PUPPETDB_URL=https://puppetdb.example.com:8081
# X-Authentication token
#PUPPETDB_AUTH_TOKEN=replace-me
# Puppet environment
#PUPPETDB_ENVIRONMENT=replace-me
# Import deactivated and expired nodes
#PUPPETDB_INCLUDE_INACTIVE=false
# Import package inventory
#PUPPETDB_INCLUDE_PACKAGES=false
# Additional fact names
#PUPPETDB_EXTRA_FACTS=replace-me
# Nodes per page
#PUPPETDB_PAGE_SIZE=500
# Fact rows per page
#PUPPETDB_FACT_PAGE_SIZE=2000
# User-Agent
#PUPPETDB_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#PUPPETDB_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#PUPPETDB_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#PUPPETDB_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#PUPPETDB_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#PUPPETDB_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#PUPPETDB_EXPECT='asset_count>=1'

# --- Quest KACE SMA (quest-kace) --------------------------------------------
# KACE SMA URL (required)
#QUEST_KACE_URL=https://kace.example.com
# Organization name
#QUEST_KACE_ORGANIZATION=replace-me
# Username (required)
#QUEST_KACE_USERNAME=replace-me
# Password (required)
#QUEST_KACE_PASSWORD=replace-me
# Import installed software
#QUEST_KACE_IMPORT_SOFTWARE=true
# Enrich from the asset CMDB
#QUEST_KACE_IMPORT_ASSETS=true
# Records per page
#QUEST_KACE_PAGE_SIZE=100
# User-Agent
#QUEST_KACE_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#QUEST_KACE_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#QUEST_KACE_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#QUEST_KACE_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#QUEST_KACE_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#QUEST_KACE_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#QUEST_KACE_EXPECT='asset_count>=1'

# --- Red Hat Insights (redhat-insights) -------------------------------------
# Console URL
#REDHAT_INSIGHTS_URL=https://console.redhat.com
# Red Hat SSO URL
#REDHAT_INSIGHTS_SSO_URL=https://sso.redhat.com
# Service account client ID
#REDHAT_INSIGHTS_CLIENT_ID=replace-me
# Service account client secret
#REDHAT_INSIGHTS_CLIENT_SECRET=replace-me
# x-rh-identity header
#REDHAT_INSIGHTS_IDENTITY_HEADER=replace-me
# Token scope
#REDHAT_INSIGHTS_SCOPE=api.console
# Import installed packages
#REDHAT_INSIGHTS_INCLUDE_SOFTWARE=false
# Import CVE findings
#REDHAT_INSIGHTS_INCLUDE_VULNERABILITIES=false
# CVE enrichment limit
#REDHAT_INSIGHTS_VULNERABILITY_LIMIT=500
# Systems per page
#REDHAT_INSIGHTS_PAGE_SIZE=50
# User-Agent
#REDHAT_INSIGHTS_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#REDHAT_INSIGHTS_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#REDHAT_INSIGHTS_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#REDHAT_INSIGHTS_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#REDHAT_INSIGHTS_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#REDHAT_INSIGHTS_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#REDHAT_INSIGHTS_EXPECT='asset_count>=1'

# --- Scan Passive Assets (runzero-scan-passive-assets) ----------------------
# runZero URL (required)
#RUNZERO_SCAN_PASSIVE_ASSETS_URL=https://vendor.example.com
# Target site ID (required)
#RUNZERO_SCAN_PASSIVE_ASSETS_SITE_ID=replace-me
# runZero org API token (required)
#RUNZERO_SCAN_PASSIVE_ASSETS_ORG_API_TOKEN=replace-me
# User-Agent
#RUNZERO_SCAN_PASSIVE_ASSETS_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#RUNZERO_SCAN_PASSIVE_ASSETS_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#RUNZERO_SCAN_PASSIVE_ASSETS_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#RUNZERO_SCAN_PASSIVE_ASSETS_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#RUNZERO_SCAN_PASSIVE_ASSETS_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#RUNZERO_SCAN_PASSIVE_ASSETS_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#RUNZERO_SCAN_PASSIVE_ASSETS_EXPECT='asset_count>=1'

# --- runZero Task Sync (runzero-task-sync) ----------------------------------
# Source runZero URL
#RUNZERO_TASK_SYNC_SRC_URL=https://vendor.example.com
# Source org ID (required)
#RUNZERO_TASK_SYNC_SRC_ORG_ID=replace-me
# Source task search filter
#RUNZERO_TASK_SYNC_SRC_TASK_SEARCH_FILTER=name:="test"
# Destination runZero URL
#RUNZERO_TASK_SYNC_DST_URL=https://vendor.example.com
# Destination org ID (required)
#RUNZERO_TASK_SYNC_DST_ORG_ID=replace-me
# Destination site ID (required)
#RUNZERO_TASK_SYNC_DST_SITE_ID=replace-me
# Hide source tasks after sync
#RUNZERO_TASK_SYNC_HIDE_TASKS_ON_SYNC=false
# Source API token (required)
#RUNZERO_TASK_SYNC_SRC_API_TOKEN=replace-me
# Destination API token (required)
#RUNZERO_TASK_SYNC_DST_API_TOKEN=replace-me
# User-Agent
#RUNZERO_TASK_SYNC_DST_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#RUNZERO_TASK_SYNC_DST_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#RUNZERO_TASK_SYNC_DST_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#RUNZERO_TASK_SYNC_DST_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#RUNZERO_TASK_SYNC_DST_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#RUNZERO_TASK_SYNC_DST_TLS_CLIENT_KEY=replace-me
# User-Agent
#RUNZERO_TASK_SYNC_SRC_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#RUNZERO_TASK_SYNC_SRC_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#RUNZERO_TASK_SYNC_SRC_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#RUNZERO_TASK_SYNC_SRC_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#RUNZERO_TASK_SYNC_SRC_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#RUNZERO_TASK_SYNC_SRC_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#RUNZERO_TASK_SYNC_EXPECT='asset_count>=1'

# --- Vulnerability Workflow (runzero-vulnerability-workflow) ----------------
# runZero source URL (required)
#RUNZERO_VULNERABILITY_WORKFLOW_SRC_URL=https://vendor.example.com
# Workflow endpoint URL (required)
#RUNZERO_VULNERABILITY_WORKFLOW_DST_URL=https://vendor.example.com
# runZero API token (required)
#RUNZERO_VULNERABILITY_WORKFLOW_RUNZERO_API_TOKEN=replace-me
# User-Agent
#RUNZERO_VULNERABILITY_WORKFLOW_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#RUNZERO_VULNERABILITY_WORKFLOW_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#RUNZERO_VULNERABILITY_WORKFLOW_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#RUNZERO_VULNERABILITY_WORKFLOW_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#RUNZERO_VULNERABILITY_WORKFLOW_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#RUNZERO_VULNERABILITY_WORKFLOW_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#RUNZERO_VULNERABILITY_WORKFLOW_EXPECT='asset_count>=1'

# --- Scale Computing (scale-computing) --------------------------------------
# Cluster base URL (required)
#SCALE_COMPUTING_BASE_URL=https://hc3.example.com
# Username (required)
#SCALE_COMPUTING_USERNAME=replace-me
# Password (required)
#SCALE_COMPUTING_PASSWORD=replace-me
# User-Agent
#SCALE_COMPUTING_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#SCALE_COMPUTING_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#SCALE_COMPUTING_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#SCALE_COMPUTING_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#SCALE_COMPUTING_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#SCALE_COMPUTING_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#SCALE_COMPUTING_EXPECT='asset_count>=1'

# --- Slurp'it (slurpit) -----------------------------------------------------
# Slurp'it URL (required)
#SLURPIT_URL=https://slurpit.example.com
# API key (required)
#SLURPIT_API_KEY=replace-me
# Include disabled devices
#SLURPIT_INCLUDE_DISABLED=false
# Collect site details
#SLURPIT_COLLECT_SITES=true
# Page size
#SLURPIT_PAGE_SIZE=512
# Maximum devices
#SLURPIT_MAX_DEVICES=50000
# User-Agent
#SLURPIT_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#SLURPIT_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#SLURPIT_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#SLURPIT_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#SLURPIT_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#SLURPIT_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#SLURPIT_EXPECT='asset_count>=1'

# --- Snipe-IT (snipe-it) ----------------------------------------------------
# Snipe-IT URL (required)
#SNIPE_IT_URL=https://snipeit.example.com
# API token (required)
#SNIPE_IT_API_TOKEN=replace-me
# User-Agent
#SNIPE_IT_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#SNIPE_IT_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#SNIPE_IT_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#SNIPE_IT_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#SNIPE_IT_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#SNIPE_IT_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#SNIPE_IT_EXPECT='asset_count>=1'

# --- Snow License Manager (snow-license-manager) ----------------------------
# Snow base URL (required)
#SNOW_LICENSE_MANAGER_URL=https://snow.example.com
# Customer ID (required)
#SNOW_LICENSE_MANAGER_CUSTOMER_ID=replace-me
# Username (required)
#SNOW_LICENSE_MANAGER_USERNAME=replace-me
# Password (required)
#SNOW_LICENSE_MANAGER_PASSWORD=replace-me
# User-Agent
#SNOW_LICENSE_MANAGER_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#SNOW_LICENSE_MANAGER_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#SNOW_LICENSE_MANAGER_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#SNOW_LICENSE_MANAGER_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#SNOW_LICENSE_MANAGER_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#SNOW_LICENSE_MANAGER_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#SNOW_LICENSE_MANAGER_EXPECT='asset_count>=1'

# --- SolarWinds Information Service (solarwinds-information-service) --------
# SWIS URL
#SOLARWINDS_INFORMATION_SERVICE_URL=https://vendor.example.com
# Username (required)
#SOLARWINDS_INFORMATION_SERVICE_USERNAME=replace-me
# Password (required)
#SOLARWINDS_INFORMATION_SERVICE_PASSWORD=replace-me
# User-Agent
#SOLARWINDS_INFORMATION_SERVICE_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#SOLARWINDS_INFORMATION_SERVICE_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#SOLARWINDS_INFORMATION_SERVICE_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#SOLARWINDS_INFORMATION_SERVICE_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#SOLARWINDS_INFORMATION_SERVICE_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#SOLARWINDS_INFORMATION_SERVICE_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#SOLARWINDS_INFORMATION_SERVICE_EXPECT='asset_count>=1'

# --- Sophos Central (sophos-central) ----------------------------------------
# Sophos Central API URL (required)
#SOPHOS_CENTRAL_URL=https://api.central.sophos.com
# Sophos ID authentication URL
#SOPHOS_CENTRAL_AUTH_URL=https://vendor.example.com
# Tenant IDs
#SOPHOS_CENTRAL_TENANT_IDS=replace-me
# API credential client ID (required)
#SOPHOS_CENTRAL_CLIENT_ID=replace-me
# API credential client secret (required)
#SOPHOS_CENTRAL_CLIENT_SECRET=replace-me
# Page size
#SOPHOS_CENTRAL_PAGE_SIZE=100
# User-Agent
#SOPHOS_CENTRAL_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#SOPHOS_CENTRAL_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#SOPHOS_CENTRAL_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#SOPHOS_CENTRAL_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#SOPHOS_CENTRAL_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#SOPHOS_CENTRAL_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#SOPHOS_CENTRAL_EXPECT='asset_count>=1'

# --- Stairwell (stairwell) --------------------------------------------------
# Stairwell API URL
#STAIRWELL_URL=https://app.stairwell.com
# Environment ID (required)
#STAIRWELL_ENVIRONMENT_ID=replace-me
# API token (required)
#STAIRWELL_API_TOKEN=replace-me
# User-Agent
#STAIRWELL_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#STAIRWELL_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#STAIRWELL_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#STAIRWELL_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#STAIRWELL_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#STAIRWELL_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#STAIRWELL_EXPECT='asset_count>=1'

# --- Sumo Logic (sumo-logic) ------------------------------------------------
# runZero source URL (required)
#SUMO_LOGIC_SRC_URL=https://vendor.example.com
# Sumo HTTP endpoint (required)
#SUMO_LOGIC_DST_URL=https://vendor.example.com
# runZero export token (required)
#SUMO_LOGIC_RUNZERO_EXPORT_TOKEN=replace-me
# User-Agent
#SUMO_LOGIC_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#SUMO_LOGIC_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#SUMO_LOGIC_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#SUMO_LOGIC_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#SUMO_LOGIC_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#SUMO_LOGIC_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#SUMO_LOGIC_EXPECT='asset_count>=1'

# --- Synology DSM (synology-dsm) --------------------------------------------
# DSM URL (required)
#SYNOLOGY_DSM_URL=https://nas.example.com:5001
# Username (required)
#SYNOLOGY_DSM_USERNAME=replace-me
# Password (required)
#SYNOLOGY_DSM_PASSWORD=replace-me
# One-time password
#SYNOLOGY_DSM_OTP_CODE=replace-me
# Collect Virtual Machine Manager guests
#SYNOLOGY_DSM_COLLECT_VMS=true
# Collect Surveillance Station cameras
#SYNOLOGY_DSM_COLLECT_CAMERAS=true
# Collect Active Backup for Business devices
#SYNOLOGY_DSM_COLLECT_BACKUP_DEVICES=true
# Collect DHCP leases
#SYNOLOGY_DSM_COLLECT_DHCP_LEASES=true
# DHCP interfaces
#SYNOLOGY_DSM_DHCP_INTERFACES=bond0,eth0,ovs_eth0,eth1
# Maximum assets
#SYNOLOGY_DSM_MAX_ASSETS=5000
# User-Agent
#SYNOLOGY_DSM_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#SYNOLOGY_DSM_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#SYNOLOGY_DSM_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#SYNOLOGY_DSM_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#SYNOLOGY_DSM_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#SYNOLOGY_DSM_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#SYNOLOGY_DSM_EXPECT='asset_count>=1'

# --- Tactical RMM (tactical-rmm) --------------------------------------------
# Tactical RMM API URL (required)
#TACTICAL_RMM_URL=https://api.example.com
# API key (required)
#TACTICAL_RMM_API_KEY=replace-me
# Agent type
#TACTICAL_RMM_MONITORING_TYPE=all
# Import installed software
#TACTICAL_RMM_EXTRACT_SOFTWARE=false
# Maximum agents to query for software
#TACTICAL_RMM_SOFTWARE_AGENT_LIMIT=250
# User-Agent
#TACTICAL_RMM_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#TACTICAL_RMM_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#TACTICAL_RMM_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#TACTICAL_RMM_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#TACTICAL_RMM_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#TACTICAL_RMM_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#TACTICAL_RMM_EXPECT='asset_count>=1'

# --- Tailscale (tailscale) --------------------------------------------------
# Tailscale API URL
#TAILSCALE_URL=https://vendor.example.com
# Tailnet (required)
#TAILSCALE_TAILNET=replace-me
# OAuth client ID
#TAILSCALE_CLIENT_ID=replace-me
# API key / OAuth client secret (required)
#TAILSCALE_API_KEY_OR_CLIENT_SECRET=replace-me
# User-Agent
#TAILSCALE_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#TAILSCALE_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#TAILSCALE_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#TAILSCALE_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#TAILSCALE_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#TAILSCALE_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#TAILSCALE_EXPECT='asset_count>=1'

# --- TP-Link Omada (tp-link-omada) ------------------------------------------
# Omada controller URL (required)
#TP_LINK_OMADA_URL=https://omada.example.com:8043
# Omada controller ID (omadacId) (required)
#TP_LINK_OMADA_OMADAC_ID=replace-me
# Open API client ID (required)
#TP_LINK_OMADA_CLIENT_ID=replace-me
# Open API client secret (required)
#TP_LINK_OMADA_CLIENT_SECRET=replace-me
# Site IDs
#TP_LINK_OMADA_SITE_IDS=replace-me
# Import Omada devices
#TP_LINK_OMADA_EXTRACT_DEVICES=true
# Import clients
#TP_LINK_OMADA_EXTRACT_CLIENTS=true
# Page size
#TP_LINK_OMADA_PAGE_SIZE=100
# Maximum pages per collection
#TP_LINK_OMADA_MAX_PAGES=200
# User-Agent
#TP_LINK_OMADA_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#TP_LINK_OMADA_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#TP_LINK_OMADA_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#TP_LINK_OMADA_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#TP_LINK_OMADA_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#TP_LINK_OMADA_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#TP_LINK_OMADA_EXPECT='asset_count>=1'

# --- Trellix ePolicy Orchestrator (trellix-epo) -----------------------------
# ePolicy Orchestrator URL (required)
#TRELLIX_EPO_URL=https://epo.example.com:8443
# Username (required)
#TRELLIX_EPO_USERNAME=replace-me
# Password (required)
#TRELLIX_EPO_PASSWORD=replace-me
# Collection mode
#TRELLIX_EPO_COLLECTION_MODE=search
# Search text
#TRELLIX_EPO_SEARCH_TEXT=replace-me
# Import unmanaged systems
#TRELLIX_EPO_INCLUDE_UNMANAGED=true
# User-Agent
#TRELLIX_EPO_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#TRELLIX_EPO_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#TRELLIX_EPO_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#TRELLIX_EPO_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#TRELLIX_EPO_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#TRELLIX_EPO_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#TRELLIX_EPO_EXPECT='asset_count>=1'

# --- Trend Micro Vision One (trend-vision-one) ------------------------------
# Vision One API host (required)
#TREND_VISION_ONE_API_HOST=https://api.xdr.trendmicro.com
# API key (required)
#TREND_VISION_ONE_API_TOKEN=replace-me
# Inventory filter
#TREND_VISION_ONE_INVENTORY_FILTER=replace-me
# Import Trend agent products as software
#TREND_VISION_ONE_IMPORT_SOFTWARE=true
# Fetch MAC addresses
#TREND_VISION_ONE_FETCH_INTERFACES=false
# MAC address lookup limit
#TREND_VISION_ONE_INTERFACE_LIMIT=1000
# Page size
#TREND_VISION_ONE_PAGE_SIZE=100
# User-Agent
#TREND_VISION_ONE_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#TREND_VISION_ONE_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#TREND_VISION_ONE_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#TREND_VISION_ONE_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#TREND_VISION_ONE_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#TREND_VISION_ONE_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#TREND_VISION_ONE_EXPECT='asset_count>=1'

# --- TrueNAS (truenas) ------------------------------------------------------
# TrueNAS URL (required)
#TRUENAS_URL=https://truenas.example.com
# API key (required)
#TRUENAS_API_KEY=replace-me
# Collect virtual machines
#TRUENAS_COLLECT_VMS=true
# Collect applications
#TRUENAS_COLLECT_APPS=true
# Collect pools and disks
#TRUENAS_COLLECT_STORAGE=true
# Page size
#TRUENAS_PAGE_SIZE=100
# Maximum records per collection
#TRUENAS_MAX_RECORDS=5000
# User-Agent
#TRUENAS_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#TRUENAS_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#TRUENAS_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#TRUENAS_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#TRUENAS_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#TRUENAS_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#TRUENAS_EXPECT='asset_count>=1'

# --- Ubiquiti UniFi Network (ubiquiti-unifi-network) ------------------------
# UniFi controller URL (required)
#UNIFI_URL=https://controller.example.com
# Site name
#UNIFI_SITE_NAME=Default
# Extract clients
#UNIFI_EXTRACT_CLIENTS=true
# Extract devices
#UNIFI_EXTRACT_DEVICES=true
# Client API filter
#UNIFI_CLIENT_API_FILTER=replace-me
# Page size
#UNIFI_PAGE_LIMIT=100
# API key (required)
#UNIFI_API_KEY=replace-me
# User-Agent
#UNIFI_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#UNIFI_TLS_INSECURE=false
# Additional CA certs (PEM)
#UNIFI_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#UNIFI_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#UNIFI_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#UNIFI_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#UNIFI_EXPECT='asset_count>=1'

# --- Ubiquiti UniFi Protect (ubiquiti-unifi-protect) ------------------------
# UniFi console URL (required)
#UBIQUITI_UNIFI_PROTECT_URL=https://192.0.2.1
# UniFi API key (required)
#UBIQUITI_UNIFI_PROTECT_API_KEY=replace-me
# Collect cameras
#UBIQUITI_UNIFI_PROTECT_COLLECT_CAMERAS=true
# Collect the NVR
#UBIQUITI_UNIFI_PROTECT_COLLECT_NVR=true
# Collect sensors
#UBIQUITI_UNIFI_PROTECT_COLLECT_SENSORS=true
# Collect lights, chimes, and viewers
#UBIQUITI_UNIFI_PROTECT_COLLECT_ACCESSORIES=true
# Collect speakers, sirens, relays, fobs, bridges, link stations, and alarm hubs
#UBIQUITI_UNIFI_PROTECT_COLLECT_EXTENDED=false
# Include disconnected devices
#UBIQUITI_UNIFI_PROTECT_INCLUDE_DISCONNECTED=true
# Maximum devices
#UBIQUITI_UNIFI_PROTECT_MAX_DEVICES=5000
# User-Agent
#UBIQUITI_UNIFI_PROTECT_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#UBIQUITI_UNIFI_PROTECT_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#UBIQUITI_UNIFI_PROTECT_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#UBIQUITI_UNIFI_PROTECT_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#UBIQUITI_UNIFI_PROTECT_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#UBIQUITI_UNIFI_PROTECT_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#UBIQUITI_UNIFI_PROTECT_EXPECT='asset_count>=1'

# --- Ubiquiti UniFi Site Manager (ubiquiti-unifi-site-manager) --------------
# Site Manager API URL
#UBIQUITI_UNIFI_SITE_MANAGER_URL=https://api.ui.com
# API key (required)
#UBIQUITI_UNIFI_SITE_MANAGER_API_KEY=replace-me
# Import UniFi devices
#UBIQUITI_UNIFI_SITE_MANAGER_EXTRACT_DEVICES=true
# Read console detail
#UBIQUITI_UNIFI_SITE_MANAGER_EXTRACT_HOSTS=true
# Attach site names
#UBIQUITI_UNIFI_SITE_MANAGER_EXTRACT_SITES=true
# Page size
#UBIQUITI_UNIFI_SITE_MANAGER_PAGE_SIZE=100
# Maximum pages per collection
#UBIQUITI_UNIFI_SITE_MANAGER_MAX_PAGES=200
# User-Agent
#UBIQUITI_UNIFI_SITE_MANAGER_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#UBIQUITI_UNIFI_SITE_MANAGER_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#UBIQUITI_UNIFI_SITE_MANAGER_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#UBIQUITI_UNIFI_SITE_MANAGER_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#UBIQUITI_UNIFI_SITE_MANAGER_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#UBIQUITI_UNIFI_SITE_MANAGER_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#UBIQUITI_UNIFI_SITE_MANAGER_EXPECT='asset_count>=1'

# --- Unraid (unraid) --------------------------------------------------------
# Unraid URL (required)
#UNRAID_URL=http://tower.local
# API key (required)
#UNRAID_API_KEY=replace-me
# Collect Docker containers
#UNRAID_COLLECT_CONTAINERS=true
# Import container published ports as services
#UNRAID_CONTAINER_SERVICES=true
# Include stopped containers
#UNRAID_INCLUDE_STOPPED_CONTAINERS=false
# Include containers on the default Docker bridge
#UNRAID_INCLUDE_NAT_CONTAINERS=false
# Collect virtual machines
#UNRAID_COLLECT_VMS=true
# Collect array and disk detail
#UNRAID_COLLECT_STORAGE=true
# Maximum containers
#UNRAID_MAX_CONTAINERS=500
# User-Agent
#UNRAID_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#UNRAID_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#UNRAID_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#UNRAID_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#UNRAID_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#UNRAID_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#UNRAID_EXPECT='asset_count>=1'

# --- Uptycs (uptycs) --------------------------------------------------------
# Uptycs URL (required)
#UPTYCS_URL=https://tenant.uptycs.io
# Customer ID (required)
#UPTYCS_CUSTOMER_ID=replace-me
# API key (required)
#UPTYCS_API_KEY=replace-me
# API secret (required)
#UPTYCS_API_SECRET=replace-me
# Import MAC addresses, IP addresses, and serials
#UPTYCS_INCLUDE_HOST_DETAILS=true
# Import software inventory
#UPTYCS_INCLUDE_SOFTWARE=true
# Import listening services
#UPTYCS_INCLUDE_SERVICES=false
# Import vulnerabilities
#UPTYCS_INCLUDE_VULNERABILITIES=false
# Vulnerability table name
#UPTYCS_VULNERABILITY_TABLE=upt_vulnerabilities_state
# Enrichment lookback (days)
#UPTYCS_LOOKBACK_DAYS=3
# User-Agent
#UPTYCS_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#UPTYCS_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#UPTYCS_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#UPTYCS_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#UPTYCS_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#UPTYCS_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#UPTYCS_EXPECT='asset_count>=1'

# --- Wazuh (wazuh) ----------------------------------------------------------
# Wazuh API URL
#WAZUH_URL=https://wazuh-manager:55000
# Wazuh manager hostname or IP
#WAZUH_HOSTNAME=wazuh-manager or 10.1.2.3
# Wazuh API port
#WAZUH_PORT=55000
# Username (required)
#WAZUH_USERNAME=replace-me
# Password (required)
#WAZUH_PASSWORD=replace-me
# User-Agent
#WAZUH_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#WAZUH_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#WAZUH_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#WAZUH_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#WAZUH_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#WAZUH_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#WAZUH_EXPECT='asset_count>=1'

# --- Windows SMB shares (windows-smb-shares) --------------------------------
# Target host (required)
#WINDOWS_SMB_SHARES_HOST=replace-me
# SMB port
#WINDOWS_SMB_SHARES_PORT=445
# Username (may include DOMAIN\user) (required)
#WINDOWS_SMB_SHARES_USERNAME=replace-me
# Password
#WINDOWS_SMB_SHARES_PASSWORD=replace-me
# NTLM hash (hex)
#WINDOWS_SMB_SHARES_NT_HASH=replace-me
# Domain (optional)
#WINDOWS_SMB_SHARES_DOMAIN=replace-me
# Connection timeout (seconds)
#WINDOWS_SMB_SHARES_TIMEOUT=30
# What a valid result looks like (required to run; see tests/README.md)
#WINDOWS_SMB_SHARES_EXPECT='asset_count>=1'

# --- Windows WMI (windows-wmi) ----------------------------------------------
# Host (required)
#WINDOWS_WMI_HOST=replace-me
# Username (required)
#WINDOWS_WMI_USERNAME=replace-me
# Password (required)
#WINDOWS_WMI_PASSWORD=replace-me
# Transport
#WINDOWS_WMI_TRANSPORT=winrm-https
# WinRM port
#WINDOWS_WMI_WINRM_PORT=0
# Skip TLS verification (NOT for production)
#WINDOWS_WMI_WINRM_INSECURE=false
# PEM CA bundle
#WINDOWS_WMI_WINRM_CA_CERT=replace-me
# WinRM auth
#WINDOWS_WMI_WINRM_AUTH=ntlm
# WMI namespace
#WINDOWS_WMI_WMI_NAMESPACE=//./root/cimv2
# Timeout (seconds)
#WINDOWS_WMI_TIMEOUT=60
# What a valid result looks like (required to run; see tests/README.md)
#WINDOWS_WMI_EXPECT='asset_count>=1'

# --- Workspace ONE UEM (workspace-one-uem) ----------------------------------
# Workspace ONE UEM API URL (required)
#WORKSPACE_ONE_UEM_URL=https://asXXX.awmdm.com
# API username (required)
#WORKSPACE_ONE_UEM_USERNAME=replace-me
# API password (required)
#WORKSPACE_ONE_UEM_PASSWORD=replace-me
# API key (tenant code) (required)
#WORKSPACE_ONE_UEM_API_KEY=replace-me
# Organization group ID
#WORKSPACE_ONE_UEM_ORGANIZATION_GROUP_ID=replace-me
# Import pending OS updates
#WORKSPACE_ONE_UEM_INCLUDE_OS_UPDATES=false
# Import installed applications
#WORKSPACE_ONE_UEM_INCLUDE_SOFTWARE=false
# Per-device detail limit
#WORKSPACE_ONE_UEM_DETAIL_DEVICE_LIMIT=250
# Device page size
#WORKSPACE_ONE_UEM_PAGE_SIZE=500
# User-Agent
#WORKSPACE_ONE_UEM_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#WORKSPACE_ONE_UEM_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#WORKSPACE_ONE_UEM_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#WORKSPACE_ONE_UEM_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#WORKSPACE_ONE_UEM_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#WORKSPACE_ONE_UEM_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#WORKSPACE_ONE_UEM_EXPECT='asset_count>=1'

# --- Zabbix (zabbix) --------------------------------------------------------
# Zabbix frontend URL (required)
#ZABBIX_URL=https://zabbix.example.com
# API token (required)
#ZABBIX_API_TOKEN=replace-me
# Host group filter
#ZABBIX_HOST_GROUPS=replace-me
# Include disabled hosts
#ZABBIX_INCLUDE_DISABLED=false
# Import interfaces as services
#ZABBIX_INCLUDE_SERVICES=true
# Import inventory software fields
#ZABBIX_INCLUDE_SOFTWARE=true
# Host groups per request
#ZABBIX_GROUP_CHUNK=20
# Maximum hosts
#ZABBIX_MAX_HOSTS=20000
# User-Agent
#ZABBIX_HTTP_USER_AGENT=replace-me
# Disable TLS validation
#ZABBIX_TLS_DISABLE_VALIDATION=false
# Additional CA certs (PEM)
#ZABBIX_TLS_CA_CERT=replace-me
# Pinned peer cert SHA-256
#ZABBIX_TLS_PEER_HASH=replace-me
# Client certificate (PEM)
#ZABBIX_TLS_CLIENT_CERT=replace-me
# Client key (PEM)
#ZABBIX_TLS_CLIENT_KEY=replace-me
# What a valid result looks like (required to run; see tests/README.md)
#ZABBIX_EXPECT='asset_count>=1'

