{
  "name": "empty",
  "integration": "netdisco",
  "description": "A Netdisco install that has been deployed but has not yet discovered anything. Login succeeds and the device search returns the empty array Netdisco substitutes for an empty body (its API after-hook sets the response content to '[]' whenever a handler returns nothing). The run must report zero assets and say so, not fail, and must not go on to request nodes for devices that do not exist.",
  "kwargs": {
    "url": "$BASE",
    "username": "fixture-user",
    "password": "fixture-password"
  },
  "routes": [
    {
      "name": "login",
      "match": {"method": "POST", "path": "/login"},
      "responses": [{"status": 200, "json": {"api_key": "aaaabbbbccccddddeeeeffff00001111"}}],
      "when_exhausted": "error"
    },
    {
      "name": "device-search",
      "match": {"method": "GET", "path": "/api/v1/search/device"},
      "responses": [{"status": 200, "json": []}],
      "when_exhausted": "repeat_last"
    }
  ],
  "default_response": {"status": 404, "json": {"error": "no fixture route"}},
  "expect": {
    "asset_count": 0,
    "request_count": 2,
    "requests_absent": [
      {"path_contains": "/nodes"},
      {"path_contains": "/ipinventory"}
    ],
    "log_contains": ["reported 0 devices", "no assets retrieved"]
  },
  "check_determinism": true
}
